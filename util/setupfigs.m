function setupfigs(varargin)
% SETUPFIGS Setup figures for better default output
%
% Use Nick Trefethen's figure setup calls to set reasonable defaults for 
% Matlab's line width's and font sizes
%

set(0, ...
    'defaultaxesfontsize',   12, ...
    'defaultaxeslinewidth',   0.7, ...
    'defaultlinelinewidth',   1.8, ...
    'defaultpatchlinewidth',  0.7);

%set(0,'DefaultAxesFontSize',10);
%set(0,'DefaultAxesLineWidth',1);
%set(0,'DefaultLineLineWidth',1.5);
  
